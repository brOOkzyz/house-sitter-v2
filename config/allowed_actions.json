{
  "schema_version": "1.0",
  "limits": {
    "max_steps": 5
  },
  "actions": {
    "navigate_to_waypoint": {
      "description": "Navigate to a named waypoint. Phase 1 only validates and prints this action.",
      "required_parameters": ["waypoint"],
      "parameters": {
        "waypoint": {"type": "string", "source": "semantic_waypoints"}
      }
    }
  }
}

